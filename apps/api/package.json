{
  "name": "@krubot/api",
  "private": true,
  "version": "0.1.0",
  "description": "The Kru Bot API: bots, conversations, approvals, routines, connected apps, and the box.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "echo api: runs from source on bun",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@composio/core": "^0.18.1",
    "@krubot/shared": "workspace:*",
    "better-auth": "1.7.5",
    "croner": "^9.1.0",
    "fflate": "^0.8.3",
    "hono": "^4.10.0",
    "web-push": "^3.6.7",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/web-push": "^3.6.4",
    "typescript": "^5"
  }
}
